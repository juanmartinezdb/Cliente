
const objeto = { a: 1, b: 2, c: 3 };
for (key in objeto) {
    console.log(key);
}

const claves =Object.keys(objeto) //nos devuelve un array con las "claves" del objeto.
const valores =Object.values(objeto) // nos devuelve un array con "valor" del objeto.

va a ser de encontrar en un json , mas que de mostrar